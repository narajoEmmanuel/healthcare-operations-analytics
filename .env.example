# Copy to .env. These are local-only defaults; change the password locally.
POSTGRES_DB=healthcare_analytics
POSTGRES_USER=healthcare
POSTGRES_PASSWORD=replace_with_a_local_password
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432

# Use Azurite's documented devstoreaccount1 connection string here.
# Emulator credentials are local-development configuration, not production secrets.
AZURE_STORAGE_CONNECTION_STRING=replace_with_documented_azurite_connection_string
AZURE_BLOB_CONTAINER=raw

CMS_DATASET_ID=690ddc6c-2767-4618-b277-420ffb2bf27c
CMS_REPORTING_YEAR=2024
